name: DISABLED publish-prophit-live-data

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This experimental live-data workflow is disabled. PropHit site is restored to read data/prophit_latest.json via run-model.yml."
